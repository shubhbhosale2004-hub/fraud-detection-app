# fraud-detection-app
A machine learning based fraud detection system built using Python and Streamlit. The application allows users to upload transaction data or enter it manually and predicts whether a transaction is fraudulent or not. It provides results through an interactive dashboard.
